# Cut Corners Audit

This audit separates real functionality from placeholder/scaffold systems. It is intentionally blunt so future work stays honest.

| System/component | Current state | Real / placeholder / broken | Risk | Fix applied in this pass | Remaining work |
| --- | --- | --- | --- | --- | --- |
| Memory system | SQLite command surface added for Tauri app data, with localStorage migration bridge and UI controls | Real durable foundation, build verified | Semantic/vector memory is still not wired | Added `get_memory_store_status`, `upsert_memory_records`, `list_memory_records`, durable memory UI, and migration bridge; `npx tauri build` completed successfully | Runtime-test migration inside the installed app; semantic/vector memory remains future work |
| Plugin system | Local registry, disk discovery, manifest validation, guarded execution hooks | Real foundation, execution must stay guarded | Third-party tools can be risky if executed without policy | Expanded manifest fields and connector plugin records | Add stronger backend permission enforcement and signed manifests |
| Trust layer | Verification logs, command/runtime/filesystem/process receipts | Real where commands are invoked | Receipts depend on user-triggered verification actions | Receipt UI already displays stdout/stderr/exit/path when present | Add durable receipt browser and export |
| Approval system | Agent packets with statuses and risk metadata | Real local approval queue | Local-only approval state can be lost/reset | Added risk/action/preview fields and connector route packets | Backend durable approval store |
| Ollama integration | `/api/tags` health checks and runtime proof command | Real | Ollama can still be unreachable if process/CORS not configured | Existing diagnostics remain visible | Add guided startup detector and model pull instructions |
| Agent routing | Jose/Hector/Miya/Alphonso packets in local bus | Real local packet routing | Not an autonomous scheduler | Added Shayan -> Jose command intake, Jose task distribution, agent report-back packets, and Jose confirmation ledger | Backend task runner and durable queues |
| Jose orchestration | Orchestrator workspace with workload, approvals, handoffs, analytics | Real UI over local packets; some analytics heuristic | Duplicate/conflict detection is not semantic | Labeled heuristics and placeholders | Implement semantic task dedupe and conflict rules |
| Miya generation pipeline | Template package generation and optional Ollama enrichment | Real template, optional model call | External media generation not connected | Export readiness labels added; creative handoff now routes Miya -> Jose for review instead of direct execution | Add file export and local image/video tool adapters |
| Hector research | Live source discovery + source proof fetching in Tauri backend; Hector can discover public URLs for a query and verify reachable sources with citation proof | Real live research backend | Search depends on network availability and can still return empty results for some queries | Added `search_research_sources` (DuckDuckGo HTML discovery), upgraded Hector run flow to discover+fetch, and updated run logs/citation UX | Add provider failover and query refinement helpers; keep all external account actions Jose-gated |
| Telegram bridge | Connector status and local route packet simulation | Placeholder/scaffold only for live transport | No actual messages sent/received | Added setup docs, env names, route packet foundation | Implement Bot API polling/webhook backend |
| WhatsApp bridge | Connector status and local route packet simulation | Placeholder/scaffold only for live transport | No actual messages sent/received | Added setup docs and official-provider-only rules | Implement Cloud API/Twilio webhook handler |
| Security/secrets | No root `.env.example` or root `.gitignore` before this pass | Weak | Tokens could be committed accidentally | Added `.env.example` and `.gitignore` protections | Add secret scanner/preflight |
| Build scripts | Vite, Vitest, Tauri scripts | Real | PowerShell may require `npm.cmd` | Baseline build verified | Add one-command release preflight |
| Tauri config | Tauri v2 config, identifier, frontend dist | Real | Manual launch not verified in this pass | Baseline and final bundle builds run | Add automated desktop smoke check |
| UI assets | Mascots bundled as PNGs | Real but large | Larger installer/assets | No change this pass | Optimize mascot images |
| State management | React state plus localStorage services | Real but decentralized | Multiple local ledgers can drift | Added connector/Hector services using same pattern | Consider a small local data layer abstraction |
| Logs | Verification/session/plugin/connector logs in localStorage plus audit calls where available | Real but partly temporary | Not all logs are backend durable | Added connector and Hector activity logs | Durable unified log store |
| Error handling | Many panels have empty/error states | Partial | Some runtime failures only surface in console | Added honest empty states in new panels | Add global error boundary |
| Installer output | NSIS/MSI generated by Tauri | Real | Existing installed app may need reinstall | Verified installer path after builds | Add app auto-update metadata later |
