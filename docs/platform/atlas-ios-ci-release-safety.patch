diff --git a/.github/workflows/ios-build.yml b/.github/workflows/ios-build.yml
index a1a1563..ae9647c 100644
--- a/.github/workflows/ios-build.yml
+++ b/.github/workflows/ios-build.yml
@@ -1,18 +1,30 @@
-name: Build iOS Companion
+name: iOS CI and Release

 on:
+  pull_request:
+    branches: [main]
+    paths:
+      - 'ios/**'
+      - '.github/workflows/ios-build.yml'
   push:
     branches: [main]
     paths:
       - 'ios/**'
       - '.github/workflows/ios-build.yml'
   workflow_dispatch:
+    inputs:
+      release_to_testflight:
+        description: 'After tests pass, sign, archive, export, and upload this exact ref to TestFlight.'
+        required: true
+        default: false
+        type: boolean

 permissions:
   contents: read

 jobs:
-  build:
+  test:
+    name: Native iOS tests
     runs-on: macos-15
     timeout-minutes: 40

@@ -24,7 +36,7 @@ jobs:

       - name: Select Xcode 26
         run: |
-          # Use latest available Xcode (26+) required for iOS 26 SDK
+          # Use latest available Xcode (26+) required for iOS 26 SDK.
           XCODE=$(ls /Applications | grep "^Xcode_2[6-9]" | sort -V | tail -1 || ls /Applications | grep "^Xcode" | sort -V | tail -1)
           echo "Using: $XCODE"
           sudo xcode-select -s "/Applications/$XCODE/Contents/Developer"
@@ -48,6 +60,27 @@ jobs:
             -destination "platform=iOS Simulator,id=$UDID" \
             CODE_SIGNING_ALLOWED=NO

+  release:
+    name: Signed TestFlight release
+    needs: test
+    if: ${{ github.event_name == 'workflow_dispatch' && inputs.release_to_testflight }}
+    runs-on: macos-15
+    timeout-minutes: 40
+
+    steps:
+      - name: Checkout
+        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
+        with:
+          persist-credentials: false
+
+      - name: Select Xcode 26
+        run: |
+          # Use latest available Xcode (26+) required for iOS 26 SDK.
+          XCODE=$(ls /Applications | grep "^Xcode_2[6-9]" | sort -V | tail -1 || ls /Applications | grep "^Xcode" | sort -V | tail -1)
+          echo "Using: $XCODE"
+          sudo xcode-select -s "/Applications/$XCODE/Contents/Developer"
+          xcodebuild -version
+
       - name: Setup signing
         env:
           IOS_CERT_DER: ${{ secrets.IOS_CERT_DER }}
@@ -124,7 +157,7 @@ jobs:
           cp "$PP_PATH" ~/Library/MobileDevice/Provisioning\ Profiles/"$UUID".mobileprovision

           echo "PP_NAME=$PP_NAME" >> "$GITHUB_ENV"
-          echo "PP_UUID=$UUID"    >> "$GITHUB_ENV"
+          echo "PP_UUID=$UUID" >> "$GITHUB_ENV"

       - name: Archive iOS app
         working-directory: ios/AlphonsoCompanion
@@ -231,7 +264,7 @@ jobs:
       - name: Build summary
         if: always()
         run: |
-          echo "## iOS Build Complete" >> $GITHUB_STEP_SUMMARY
+          echo "## Signed iOS Release Complete" >> $GITHUB_STEP_SUMMARY
           echo "" >> $GITHUB_STEP_SUMMARY
           echo "| | |" >> $GITHUB_STEP_SUMMARY
           echo "|---|---|" >> $GITHUB_STEP_SUMMARY
@@ -240,4 +273,4 @@ jobs:
           echo "| **Profile UUID** | ${{ env.PP_UUID }} |" >> $GITHUB_STEP_SUMMARY
           echo "" >> $GITHUB_STEP_SUMMARY
           echo "### TestFlight" >> $GITHUB_STEP_SUMMARY
-          echo "If the upload step succeeded, the build is processing on App Store Connect (usually 5–15 min)." >> $GITHUB_STEP_SUMMARY
+          echo "This upload ran only because release_to_testflight was explicitly selected." >> $GITHUB_STEP_SUMMARY
