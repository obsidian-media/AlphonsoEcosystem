node_modules/
dist/
voice/backend/venv/
__pycache__/
.pytest_cache/
coverage/
src-tauri/target/
release/
.tauri/alphonso-updater.key
.tauri-updater-key
.tauri-updater-key.pub

# License signing private key (vendor secret — never commit)
license-signing-key.private.json
*.private.json

.env
.env.*
!.env.example

npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

*.local
*.log

.vercel

# Playwright outputs
playwright-report/
test-results/

# Auto-generated by export-ground-truth
ALPHONSO_GROUND_TRUTH.generated.md
alphonso-ground-truth.snapshot.json

# RC0 proof artifacts (regenerated by proof-native-selfdev)
release/rc0/

# Claude Code ephemeral session lock
.claude/scheduled_tasks.lock
.claude/settings.local.json

# TypeScript incremental build cache
*.tsbuildinfo

# OpenCode agent config
.opencode/
.claude/worktrees/
.worktrees/

# iOS/Xcode
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*/build/
*.swp
.DS_Store
*.p8
*.p12
*.pfx
certs/
scripts/certs/

# Local planning doc — not a durable project record
ALPHONSOFORMACOSANDLINUXSPRINT.md
ALPHONSO BOARDROOM HANDOFF.pdf

# Local planning doc — references machine-specific Hermes install paths/ports
# on this dev machine only; referenced (not embedded) from TRUTH_FIRST_EXECUTION_PLAN.md,
# docs/governance/DEFERRED_WORK.md, and docs/AGENT_GUIDE.md.
docs/HERMES_AGENT_DELEGATION_PLAN.md

# Local competitive-strategy docs — internal positioning analysis, not a
# durable/public project record; referenced from TRUTH_FIRST_EXECUTION_PLAN.md.
docs/STRATEGY_AGNT_VS_ALPHONSO.md
docs/STRATEGY_AB_ROADMAP.md
docs/STRATEGY_PAPERCLIP_REFERENCE.md

# Windows folder-view metadata
desktop.ini
.superpowers/

# Repo governance — credential files and private audit notes
*credential*
audits/private/

# External/informal QA reports pasted in from other channels (e.g. Slack) —
# machine-local working notes, not a repo-governed doc; findings get triaged
# into DEFERRED_WORK.md / real issues instead of living here.
Q&A E2E Test.md

# Fetched at build time by scripts/fetch-ollama-runtime.mjs — never commit
# vendored runtime binaries (see docs/DEPENDENCY_BUNDLING_PLAN.md). The
# directory itself must stay tracked (src-tauri/vendor/ollama/.gitkeep) —
# tauri-build validates bundle.resources paths exist on EVERY cargo
# invocation (check/test/clippy/build), not only `tauri build`'s actual
# bundling step, so a fully-gitignored directory breaks plain `cargo check`
# for every contributor who hasn't run the fetch script. Learned this the
# hard way via a real CI failure (see docs/DEPENDENCY_BUNDLING_PLAN.md's
# Implementation log) — do not re-ignore the directory itself again.
src-tauri/vendor/ollama/*
!src-tauri/vendor/ollama/.gitkeep
