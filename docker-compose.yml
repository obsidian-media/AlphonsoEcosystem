services:
  whatsapp-gateway:
    build:
      context: ./gateway/whatsapp-cloud
    container_name: alphonso-whatsapp-gateway
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file: .env
